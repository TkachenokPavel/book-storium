export enum routes {
    HOME = "/",
    SEARCH = "/search",
    BOOK = "/book",
    CART = "/cart",
    ACCOUNT = "/account",
    FAVORITES = "/favorites",
    RESET = "/reset",
    SIGN_IN = "/sign-in",
    NOT_FOUND = "*",
}