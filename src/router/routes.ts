export enum ROUTE {
    HOME = "/",
    SEARCH = "search",
    BOOK_DETAILS = "book/:isbn",
    CART = "cart",
    ACCOUNT = "account",
    FAVORITES = "favorites",
    RESET = "reset",
    SIGN_IN = "sign-in",
    NOT_FOUND = "*",
}