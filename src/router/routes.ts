export enum ROUTE {
    HOME = "/",
    SEARCH = "search/:page",
    BOOK_DETAILS = "book/:isbn",
    CART = "cart",
    ACCOUNT = "account",
    FAVORITES = "favorites",
    RESET = "reset",
    AUTHENTICATION = "authentication",
    NOT_FOUND = "*",
}