import { useEffect } from "react";
import { bookAPI } from "../services";

export const Search = () => {

    // useEffect(() => {
    //     bookAPI.getSearchedBooks('java')
    //         .then(response => {
    //             console.log(response)
    //         })
    // }, [])

    return (
        <div>Search</div>
    )
}
